loom {
    accessWidenerPath = file('src/main/resources/manascore_storage.accesswidener')
}

dependencies {
    implementation(project(path: ":network-common", configuration: 'namedElements')) { transitive false }
}